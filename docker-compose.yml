services:
  zipbot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./downloads:/app/downloads
      - ./extracted:/app/extracted
      - ./rezipped:/app/rezipped
